# Copy this file to terraform.tfvars and fill in your values
# terraform.tfvars.example

# Your current public IP address with /32 suffix
# You can get it with: curl -s https://ipinfo.io/ip
my_ip = "YOUR_PUBLIC_IP/32"

# Optional: Path to your SSH public key (defaults to ~/.ssh/id_rsa.pub)
# public_key_path = "~/.ssh/id_rsa.pub"

# Optional: AWS region (defaults to eu-west-3)
# aws_region = "eu-west-3"

# Optional: Instance type (defaults to t3.small)
# instance_type = "t3.small" 